import { StyleSheet } from 'react-native';
// import fonts from '../fonts';
import colors from '../colors';

export default StyleSheet.create({
    container: {
        color: colors.GREY23COLOR,
        flexDirection: 'row',
        marginTop: 35,
        borderBottomWidth: 1.2,
        borderBottomColor: colors.THEME_COLOR,
        width: 320,
    },
});
