import React from 'react';
// import { View, Text } from 'react-native';
import Components from '../components';

// Component
const { TemplateComponents } = Components;
// Templates Component
const { ProfileTemplate } = TemplateComponents;

function Profile() {
    return (
        <ProfileTemplate />
    );
}

export default Profile;
